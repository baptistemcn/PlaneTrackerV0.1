import { makeStyles } from "@rneui/themed";

export const useGlobalStyles = makeStyles(() => ({
  layout: {
    flex: 1,
    flexDirection: "row",
    alignItems: "center",
    justifyContent: "center",
  },
}));
